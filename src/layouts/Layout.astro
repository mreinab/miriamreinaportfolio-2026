---
import '../styles/global.css';

interface Props {
  title?: string;
  chrome?: boolean;
  fonts?: boolean;
  favicon?: string;
}

const { title = 'Portfolio', chrome = true, fonts = false, favicon = '/favicon.png' } = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/png" href={favicon} />
    <title>{title}</title>
    {
      fonts && (
        <>
          <link rel="preconnect" href="https://fonts.googleapis.com" />
          <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
          <link
            href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500&display=swap"
            rel="stylesheet"
          />
        </>
      )
    }
  </head>
  <body>
    {
      chrome && (
        <header>
          <nav>
            <ul>
              <li><a href="/">Inicio</a></li>
              <li><a href="/graphicdesign">Diseño</a></li>
              <li><a href="/fashion">Moda</a></li>
              <li><a href="/webdev">Desarrollo</a></li>
            </ul>
          </nav>
        </header>
      )
    }

    <main>
      <slot />
    </main>

    {
      chrome && (
        <footer>
          <p>&copy; {new Date().getFullYear()} Portfolio</p>
        </footer>
      )
    }
  </body>
</html>

<script>
  // Mismo fade-in que usa el fondo del index: las imágenes dentro de un
  // <article> (cover de tarjeta, cover grande de detalle, galería del
  // markdown) arrancan invisibles y aparecen suavemente al cargar.
  function revealArticleImages() {
    const images = document.querySelectorAll<HTMLImageElement>('article img');

    images.forEach((img) => {
      if (img.complete) {
        img.classList.add('is-loaded');
      } else {
        img.addEventListener('load', () => img.classList.add('is-loaded'), { once: true });
      }
    });
  }

  revealArticleImages();
</script>
