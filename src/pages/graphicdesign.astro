---
import { getCollection, type CollectionEntry } from 'astro:content';
import Layout from '../layouts/Layout.astro';
import ProjectCard from '../components/ProjectCard.astro';
import SitePage from '../components/SitePage.astro';

const projects = (await getCollection('graphicdesign')).sort(
  (a: CollectionEntry<'graphicdesign'>, b: CollectionEntry<'graphicdesign'>) =>
    a.data.order - b.data.order
);
---

<Layout title="Diseño" chrome={false} favicon="/favicon-graphic.png" fonts={true}>
  <SitePage active="graphicdesign">
    <ul>
      {
        projects.map((entry) => (
          <li>
            <ProjectCard
              collection="graphicdesign"
              slug={entry.id}
              title={entry.data.title}
              cover={entry.data.cover}
              tags={entry.data.tags}
            />
          </li>
        ))
      }
    </ul>
  </SitePage>
</Layout>
